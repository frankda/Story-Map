<%= form_with model: @comment, :class => "story-form" do |f| %>
    <%= f.label :content %>
    <%= f.text_field :content %>

    <%= f.hidden_field :lat %>

    <%= f.hidden_field :lng %>

    <%= f.submit %>
<% end %>